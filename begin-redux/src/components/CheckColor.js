import React from 'react';
import { List, Map } from 'immutable';

const color_item = ({id, checked, color, onclickevent })=>{
    return (
        <li className="" style={} >
            
        </li>
    );
}